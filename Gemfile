source "https://rubygems.org"

# Use the exact stack that GitHub Pages runs in production
gem "github-pages", group: :jekyll_plugins

# Ruby 3.x needs webrick for local `jekyll serve`
gem "webrick", "~> 1.8"
